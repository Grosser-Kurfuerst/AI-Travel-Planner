# 🎤 语音输入功能使用指南

## 功能概述

在创建行程页面，用户现在可以通过语音输入旅行描述，无需手动打字。系统会自动将语音转换为文字并填充到"详细描述"字段。

## 使用方法

### 1. 进入创建行程页面

访问 `/plan` 页面或点击导航栏的"创建行程"。

### 2. 开始语音输入

在"详细描述"字段标签旁边，您会看到一个 **"语音输入"** 按钮：

```
详细描述  [🎤 语音输入]
```

点击该按钮即可开始录音。

### 3. 说出您的旅行需求

按钮变为红色的 **"停止录音"** 状态，表示正在录音：

```
详细描述  [🔇 停止录音]
🎤 正在录音中...
```

此时您可以说出旅行需求，例如：

> "我想去日本东京旅游五天，喜欢动漫文化和美食，特别想去秋叶原和浅草寺，希望品尝地道的拉面和寿司，预算五千元左右"

### 4. 停止录音

说完后，再次点击按钮停止录音。识别的文字会自动填充到文本框中。

### 5. 继续编辑或添加

- **追加内容**：可以多次使用语音输入，每次的内容会追加到现有文本后
- **手动编辑**：可以直接在文本框中修改或补充内容
- **混合使用**：可以先手动输入一部分，再用语音补充

### 6. 提交表单

完成后填写其他字段（目的地、日期、预算），点击"生成行程"。

---

## 浏览器兼容性

### ✅ 完全支持的浏览器

| 浏览器 | 版本要求 | 备注 |
|--------|----------|------|
| **Google Chrome** | 25+ | 推荐，识别准确度最高 |
| **Microsoft Edge** | 79+ | 基于 Chromium，效果很好 |
| **Opera** | 27+ | 支持良好 |

### ⚠️ 部分支持的浏览器

| 浏览器 | 说明 |
|--------|------|
| **Safari** | macOS 14.1+ / iOS 14.5+ 支持，但需用户手动授权麦克风权限 |

### ❌ 不支持的浏览器

| 浏览器 | 说明 |
|--------|------|
| **Firefox** | 目前不支持 Web Speech API |
| **旧版 IE** | 不支持现代 Web 技术 |

**如果浏览器不支持**，页面会显示提示信息：
```
ℹ️ 您的浏览器不支持语音输入，建议使用 Chrome 或 Edge 浏览器
```

---

## 权限设置

### 首次使用

第一次点击"语音输入"时，浏览器会弹出权限请求：

```
[网站] 想要使用您的麦克风
                    [拒绝] [允许]
```

**请点击"允许"** 以启用语音输入功能。

### 权限被拒绝

如果您拒绝了权限，会看到错误提示：
```
⚠️ 麦克风权限被拒绝
```

**重新授权方法**：

#### Chrome/Edge
1. 点击地址栏左侧的 🔒 图标
2. 找到"麦克风"权限
3. 选择"允许"
4. 刷新页面

#### Safari
1. Safari → 设置 → 网站 → 麦克风
2. 找到您的网站
3. 选择"允许"
4. 刷新页面

---

## 常见问题

### Q1: 为什么没有识别出任何文字？

**可能原因**：
- 环境太吵，语音不清晰
- 麦克风音量太小
- 说话速度过快

**解决方法**：
- 在安静的环境中使用
- 清晰、适中速度地说话
- 检查麦克风设置和音量

### Q2: 识别的文字不准确怎么办？

**解决方法**：
- 使用标准普通话
- 避免使用生僻词
- 说完后在文本框中手动修正
- 尝试分段说，每次说完一个完整的句子后停顿

### Q3: 为什么显示"没有检测到语音"？

**可能原因**：
- 麦克风未正常工作
- 说话音量太小
- 录音时间太短

**解决方法**：
- 测试麦克风是否工作（在系统设置中）
- 增大音量
- 持续说话至少 2-3 秒

### Q4: 可以识别方言吗？

目前系统配置为识别**标准普通话**。方言识别效果可能不理想。

### Q5: 可以识别英文吗？

可以！系统会自动识别中英文混合输入。例如：
> "我想去 Tokyo 旅游，住在 Shibuya 附近"

### Q6: 识别文字后可以撤销吗？

可以！有以下方法：
- 使用键盘 `Ctrl+Z` (Windows) 或 `Cmd+Z` (Mac) 撤销
- 手动删除不需要的文字
- 清空整个文本框重新开始

### Q7: 网络断开还能用吗？

**不能**。语音识别需要网络连接，因为：
- Chrome 使用 Google 的云端语音识别服务
- 需要将音频发送到服务器进行处理

### Q8: 我的语音数据安全吗？

- 语音数据由浏览器（Chrome/Edge）处理
- 音频不会保存在我们的服务器上
- 仅用于实时文字转换
- 建议在隐私环境中使用

---

## 技术实现

### 使用的 API

项目使用浏览器原生的 **Web Speech API**：
```typescript
const SpeechRecognition = 
  window.SpeechRecognition || 
  window.webkitSpeechRecognition;
```

### 配置参数

```typescript
recognition.continuous = true;      // 持续识别
recognition.interimResults = true;  // 显示中间结果
recognition.lang = 'zh-CN';         // 中文识别
recognition.maxAlternatives = 1;    // 只返回最佳结果
```

### 自定义 Hook

创建了 `useSpeechRecognition` Hook，提供：
- `isListening`: 是否正在录音
- `transcript`: 识别的文本
- `startListening()`: 开始录音
- `stopListening()`: 停止录音
- `resetTranscript()`: 清空文本
- `isSupported`: 浏览器是否支持
- `error`: 错误信息

---

## 使用示例

### 示例 1：简单的旅行需求

**语音输入**：
> "我想去成都旅游三天，喜欢吃火锅和看大熊猫，预算两千元"

**识别结果**：
```
我想去成都旅游三天，喜欢吃火锅和看大熊猫，预算两千元
```

### 示例 2：详细的行程描述

**语音输入**：
> "我计划下个月去云南大理和丽江玩一周，两个人一起去。我们喜欢自然风光和古镇，想体验当地的民宿。希望安排一天去洱海，一天爬苍山，还想去双廊和喜洲古镇。美食方面，想尝试白族的三道茶和酸辣鱼。预算每人五千左右。"

**识别结果**：
```
我计划下个月去云南大理和丽江玩一周，两个人一起去。我们喜欢自然风光和古镇，想体验当地的民宿。希望安排一天去洱海，一天爬苍山，还想去双廊和喜洲古镇。美食方面，想尝试白族的三道茶和酸辣鱼。预算每人五千左右。
```

### 示例 3：中英文混合

**语音输入**：
> "想去 Bangkok 曼谷旅游，住在 Sukhumvit 素坤逸路附近，喜欢 shopping 购物和 street food 街头美食"

**识别结果**：
```
想去Bangkok曼谷旅游，住在Sukhumvit素坤逸路附近，喜欢shopping购物和street food街头美食
```

---

## 最佳实践

### ✅ 推荐做法

1. **清晰表达**：用正常的说话速度，清晰地表达
2. **完整句子**：尽量说完整的句子，不要断断续续
3. **逻辑清晰**：按照"目的地 → 时间 → 喜好 → 预算"的顺序描述
4. **适当停顿**：每说完一个要点后稍作停顿
5. **及时检查**：说完后检查识别结果，及时修正错误
6. **分段输入**：如果内容很多，可以分几次说完

### ❌ 避免做法

1. 不要在嘈杂环境中使用
2. 不要离麦克风太远
3. 不要说太快或太慢
4. 不要使用过多的语气词（嗯、啊、那个等）
5. 不要中途长时间沉默（会自动停止）

---

## 隐私说明

- ✅ 语音识别在浏览器端进行
- ✅ 不会录制或存储语音文件
- ✅ 仅转换为文字后填充到表单
- ✅ 文字内容在您提交表单前不会发送到服务器
- ⚠️ Chrome 可能会将音频发送到 Google 服务器进行识别

---

## 反馈和支持

如果您在使用过程中遇到问题或有改进建议，请：
1. 检查本文档的"常见问题"部分
2. 查看浏览器控制台是否有错误信息
3. 向开发团队提交 Issue

---

**更新日期**: 2024-10-31  
**版本**: v1.0.0

